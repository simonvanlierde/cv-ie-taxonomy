node_modules
dist
*.local
.DS_Store
package-lock.json
# `tsc -b` used to leave this behind; the scripts now run `tsc --noEmit`, but a
# stray incremental build must never be committable again.
*.tsbuildinfo
